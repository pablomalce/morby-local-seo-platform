node_modules
.next
out
dist
build
.env
.env.local
.env.*.local
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp
.pnp.js
coverage
.vercel

# TypeScript incremental build info (leaks into git status otherwise)
*.tsbuildinfo

# La línea de base de ./scripts/mutar.sh: cuántos tests corren con el árbol sin
# tocar. Se regenera con `--linea-base` y es de la máquina, no del repositorio.
.mutacion-linea-base.json
