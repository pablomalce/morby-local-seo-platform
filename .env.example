# =============================================================================
# Application
# =============================================================================
NEXT_PUBLIC_APP_MODE=demo
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# Supabase (Phase 2 — DB + Auth)
# =============================================================================
# Get these from Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://llrckvopblaudpboxmtz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only — bypasses RLS, treat as a secret. Never expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# =============================================================================
# AI providers (Phase 3 — server-only)
# =============================================================================
OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=gpt-image-1
ANTHROPIC_API_KEY=
GOOGLE_GEMINI_API_KEY=

# =============================================================================
# Google integrations (Phase 4)
# =============================================================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
GOOGLE_PLACES_API_KEY=
# PageSpeed Insights (Core Web Vitals in reports). Free REST API, no OAuth/billing, ~25k req/day.
# Create at console.cloud.google.com → APIs & Services → Credentials → API key.
GOOGLE_PAGESPEED_API_KEY=
GOOGLE_BUSINESS_ACCOUNT_ID=
GOOGLE_BUSINESS_LOCATION_ID=
GOOGLE_SEARCH_CONSOLE_SITE_URL=
GA4_PROPERTY_ID=

# =============================================================================
# Observability (later)
# =============================================================================
SENTRY_DSN=
POSTHOG_KEY=

# Messaging (later — transactional emails)
RESEND_API_KEY=

# =============================================================================
# Legacy (unused — Prisma path retired in favour of Supabase JS client)
# =============================================================================
DATABASE_URL="file:./dev.db"
